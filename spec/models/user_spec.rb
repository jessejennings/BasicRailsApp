require 'rails_helper'
describe User down_vote?
    include TestFactories
    describe "#favorited(post)" do
        xit "returns `nil` if the user has not favorited the post" do
        end
    
        xit "returns the appropriate favorite if it exists" do
        end
    end
end